version: 0.2

env:
  git-credential-helper: yes
phases:
  build:
    commands:
      - echo "The code will be uploaded to S3"
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - hathor-wallet-service.zip
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
